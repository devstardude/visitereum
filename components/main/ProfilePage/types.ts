export interface Profile {
  profile: {
    name: string;
    bio: string;
    birthday: string;
    gender: string;
    address: string;
    image: any;
  };
}

export interface userData {
  name?: string;
  bio?: string;
  birthday?: string;
  gender?: string;
  address?: string;
  image?: any;
}
